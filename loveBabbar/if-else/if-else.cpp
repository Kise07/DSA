#include <iostream>
using namespace std;

int main() {
    /*
    // +ve/-ve Number Program
    int a;
    cin >> a;

    // if a is positive
    if(a>0) {
        cout << "A is Positive" << endl;
    }
    else(a<0) {
        cout << "A is Negative" << endl;
    }
    */

    /*
    // Greater Number Program
    int a, b;

    cout << "Enter the value of a: " << endl;
    cin >> a;
    cout << "Enter the value of b: " << endl;
    cin >> b;

    if(a>b) {
        cout << "A is greater" << endl;
    }
    if(b>a) {
        cout << "B is greater" << endl;
    }
    */

    // +ve/-ve/0 Number Program
    int a;
    cout << "Enter the value of a :" << endl;
    cin >> a;

    if(a>0) {
        cout << "A is positive" << endl;
    }
    else if(a<0) {
        cout << "A is negative" << endl;
    }
    else {
        cout << "A is 0" << endl;
    }

}